{
  "name": "agrotech-jetson-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1011.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "roslib": "^1.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "server": "node server.js",
    "deploy": "npm run build && echo 'Copy build/ and server.js to Jetson'"
  },
  "devDependencies": {
    "react-scripts": "^5.0.1"
  },
  "browserslist": {
    "production": [">0.2%", "not dead"],
    "development": ["last 1 chrome version"]
  },
  "proxy": "http://localhost:4500"
}
